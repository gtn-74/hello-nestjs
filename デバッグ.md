# VS Code デバッグ設定ガイド

## launch.json とは

`launch.json` は VS Code のデバッグ設定ファイルです。このファイルでデバッグの実行方法、環境変数、起動パラメータなどを定義できます。

## 基本構造

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "実行環境（node, chrome等）",
      "request": "launch または attach",
      "name": "デバッグ設定の名前"
      // その他の設定...
    }
  ]
}
```

## NestJS プロジェクト用の推奨設定

### 1. 開発サーバーのデバッグ

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "NestJS Debug",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/src/main.ts",
      "runtimeArgs": ["-r", "ts-node/register"],
      "env": {
        "NODE_ENV": "development"
      },
      "console": "integratedTerminal",
      "restart": true,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
```

### 2. 既に起動中のプロセスにアタッチ

```json
{
  "name": "Attach to NestJS",
  "type": "node",
  "request": "attach",
  "port": 9229,
  "restart": true,
  "localRoot": "${workspaceFolder}",
  "protocol": "inspector"
}
```

### 3. テストのデバッグ

```json
{
  "name": "Jest Tests",
  "type": "node",
  "request": "launch",
  "program": "${workspaceFolder}/node_modules/.bin/jest",
  "args": ["--runInBand"],
  "console": "integratedTerminal",
  "internalConsoleOptions": "neverOpen",
  "disableOptimisticBPs": true,
  "windows": {
    "program": "${workspaceFolder}/node_modules/jest/bin/jest"
  }
}
```

## 主要なプロパティ説明

### 必須プロパティ

- **type**: 実行環境（`node`, `chrome`, `python`等）
- **request**: `launch`（新しいプロセス起動）または `attach`（既存プロセスに接続）
- **name**: デバッグ設定の表示名

### よく使うプロパティ

- **program**: 実行するメインファイルのパス
- **args**: プログラムに渡す引数の配列
- **runtimeArgs**: Node.js ランタイムに渡す引数
- **env**: 環境変数の設定
- **cwd**: 作業ディレクトリ
- **console**: コンソールの種類（`integratedTerminal`, `internalConsole`）
- **restart**: ファイル変更時の自動再起動
- **skipFiles**: デバッグ時にスキップするファイルパターン

## 使い方

### 1. ブレークポイントの設置

- コードの行番号左側をクリック
- 赤い丸が表示される

### 2. デバッグの開始

- F5 キーを押す
- または「実行とデバッグ」パネルから設定を選択して実行

### 3. デバッグ操作

- **F5**: 続行（次のブレークポイントまで実行）
- **F10**: ステップオーバー（次の行へ）
- **F11**: ステップイン（関数内部へ）
- **Shift+F11**: ステップアウト（関数から出る）
- **Ctrl+Shift+F5**: 再起動
- **Shift+F5**: 停止

### 4. 変数の確認

- **変数パネル**: 現在のスコープの変数を表示
- **ウォッチ**: 特定の式を監視
- **コールスタック**: 関数の呼び出し履歴
- **ホバー**: コード上の変数にマウスを合わせて値を確認

## 実践例：NestJS コントローラーのデバッグ

1. `items.controller.ts` の `create` メソッドにブレークポイントを設置
2. F5 でデバッグ開始
3. Postman や curl で POST リクエストを送信
4. ブレークポイントで停止したら、変数の値を確認

```typescript
@Post()
create(@Body() createItemDto: CreateItemDto): itemsModel.Item {
  // ここにブレークポイントを設置
  console.log('createItemDto:', createItemDto); // この値をデバッガーで確認
  return this.itemsService.create(createItemDto);
}
```

## トラブルシューティング

### よくある問題と解決方法

1. **ブレークポイントが無効になる**
   - `outFiles` の設定を確認
   - TypeScript のソースマップが生成されているか確認

2. **デバッガーが起動しない**
   - `program` のパスが正しいか確認
   - 必要な依存関係がインストールされているか確認

3. **環境変数が読み込まれない**
   - `env` プロパティで明示的に設定
   - または `envFile` で .env ファイルを指定

## 便利なTips

- **条件付きブレークポイント**: ブレークポイントを右クリックして条件を設定
- **ログポイント**: ブレークポイントの代わりにログを出力
- **複数設定**: 異なる環境用に複数の設定を作成
- **タスクとの連携**: `preLaunchTask` でビルドタスクを事前実行

## 参考リンク

- [VS Code デバッグ公式ドキュメント](https://code.visualstudio.com/docs/editor/debugging)
- [Node.js デバッグガイド](https://code.visualstudio.com/docs/nodejs/nodejs-debugging)

## macOS + nvm 環境での注意点

### Node.js パスエラーの解決方法

nvmでNode.jsを管理している場合、VS Codeがパスを認識できないことがあります。

#### 解決方法1: runtimeExecutable を明示的に指定

```json
{
  "name": "NestJS Debug",
  "type": "node",
  "request": "launch",
  "runtimeExecutable": "/Users/username/.nvm/versions/node/v23.7.0/bin/node",
  "program": "${workspaceFolder}/src/main.ts"
}
```

#### 解決方法2: 環境変数でPATHを設定

```json
{
  "env": {
    "PATH": "/Users/username/.nvm/versions/node/v23.7.0/bin:${env:PATH}"
  }
}
```

#### 解決方法3: VS Code の設定でデフォルトを指定

`settings.json` に以下を追加：

```json
{
  "terminal.integrated.defaultProfile.osx": "zsh",
  "terminal.integrated.profiles.osx": {
    "zsh": {
      "path": "zsh",
      "args": ["-l"]
    }
  }
}
```

### 現在のNode.jsパスを確認するコマンド

```bash
which node
# または
echo $NVM_BIN
```

### nvmのデフォルトバージョンを設定

```bash
nvm alias default 23.7.0
```

これにより、新しいターミナルセッションで常に指定したバージョンが使用されます。